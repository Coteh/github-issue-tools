import 'jest';

describe('util', () => {
  describe('import', () => {
    describe('parseCSVFile', () => {
      it('performs parse for provided csv file', () => {
        fail('Not implemented');
      });
      it('rejects if invalid csv file supplied', () => {
        fail('Not implemented');
      });
      it('rejects if csv parse failed', () => {
        fail('Not implemented');
      });
    });
    describe('parseCSVHeaders', () => {
      it('performs parse for provided csv file', () => {
        fail('Not implemented');
      });
      it('rejects if invalid csv file supplied', () => {
        fail('Not implemented');
      });
      it('rejects if csv parse failed', () => {
        fail('Not implemented');
      });
    });
  });
});
